# Global rules (opencode)

- Home navigation: read `~/AGENTS.md` first (folder map, sensitive zones, tooling inventory).
- `~/Vault` = canonical knowledge base (Obsidian + git). Read it before re-deriving context on my projects; enrich it with atomic notes per `~/Vault/AGENTS.md`, then commit+push.
- 🔴 Never read or transmit `~/Documents` or `~/Downloads/_SECRETS-to-secure/`.
- All folders/files you create or rename: English names.
- Repos: prefer `jj` commands where a `.jj/` dir exists (colocated with git).
- Context discipline: models here often have small context windows — read snippets, not whole files; keep output terse.
