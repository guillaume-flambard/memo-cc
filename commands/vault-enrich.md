---
description: Promote a durable learning into the Vault as a conforming atomic note (frontmatter, MOC, commit+push)
argument-hint: <the knowledge to capture> (optional — defaults to the current context)
---

Use the **vault-enrich** skill to promote the following into `~/Vault` as a single conforming atomic
note (search first to avoid duplicates, fill frontmatter per the spec, register in the right MOC, then
commit and push). Apply the Golden Rule — if it doesn't answer a future question, say so and stop.

Knowledge to capture: $ARGUMENTS

If no argument is given, capture the most important durable learning from the current conversation.
